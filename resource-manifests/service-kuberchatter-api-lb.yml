apiVersion: v1
kind: Service
metadata:
  name: kuberchatter-api-lb
spec:
  type: LoadBalancer
  ports:
    - port: 5000
      protocol: TCP
      targetPort: 5000
  selector:
    app: kuberchatter-api

